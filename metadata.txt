# Mandatory items:

[general]
name=MdbLoader
qgisMinimumVersion=2.14
description=This plugin loads a MS Access table for use in QGIS
version=0.3
author=P. van de Geer
email=pvandegeer@gmail.com

about=This plugin loads a Microsoft Access table for use iun QGIS. The underlying "pseudo mdb provider" simulates a data provider using a memory layer.

tracker=https://github.com/pvandegeer/MdbLoader/issues
repository=https://github.com/pvandegeer/MdbLoader
# End of mandatory metadata

# Recommended items:

# Uncomment the following line and add your changelog:
changelog=* Version 0.3
 - add support for queries
 - minor improvements and fixes

# Tags are comma separated with spaces allowed
tags=database, table ms access, mdb accdb

# homepage=github.com/dummy
category=Database
icon=mdb_database.png
# experimental flag
experimental=True

# deprecated flag (applies to the whole plugin, not just a single version)
deprecated=False

